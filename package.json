{
  "name": "transposepdf",
  "version": "1.0.0",
  "description": "A mission-critical worship tool for church teams who need **reliable, offline chord transposition** during live services.",
  "main": "app.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "bash scripts/run-qa.sh",
    "qa": "bash scripts/run-qa.sh",
    "check:syntax": "node --check app.js && node --check service-worker.js && for file in modules/*.js; do node --check \"$file\"; done"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "playwright": "^1.55.0"
  }
}
